;; Indentation settings for pallet code when using emacs

;; Local Variables:
;; mode: clojure
;; eval: (define-clojure-indent (plan-fn 0) (plan-context 1) (defmethod-plan 2))
;; eval: (define-clojure-indent (defaction 'defun)(implement-action 1))
;; eval: (define-clojure-indent (plan-when 1)(plan-when-not 1))
;; eval: (define-clojure-indent (with-action-values 1)(with-service-restart 1))
;; eval: (define-clojure-indent (on-one-node 1))
;; eval: (define-clojure-indent (cluster-spec 1)(group-spec 1))
;; eval: (define-clojure-indent (with-connection 4))
;; eval: (define-clojure-indent (go-loop 1)(go-try 1)(go-tuple 1))
;; eval: (define-clojure-indent (ann 1)(All 1)(ann-protocol 1))
;; eval: (define-clojure-indent (letfn> 1))
;; eval: (define-clojure-indent (defprotocol> 1)(ann-protocol 1))
;; eval: (define-clojure-indent (doseq> 1)(fn> 1)(for> 3)(loop> 1))
;; eval: (define-clojure-indent (build-actions 1)(build-script 1)(build-plan 1))
;; eval: (define-clojure-indent (for-all 1))
;; End:
