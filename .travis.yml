language: java
jdk: openjdk6
install: echo "HELLO"
script: 
        -mvn -Dzi.version=0.5.5 clean
        -mvn compile
        -mvn -Dzi.version=0.5.5 zi:compile
        -mvn -Dzi.version=0.5.5 test

